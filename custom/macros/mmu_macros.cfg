[gcode_macro CLEAN_NOZZLE]
description: Wipe the nozzle on the brush
gcode:
    {% if printer.configfile.config["gcode_macro AFC_BRUSH"] is defined %}
      AFC_BRUSH
    {% else %}
      RESPOND MSG="No nozzle brush available — skipping"
    {% endif %}
