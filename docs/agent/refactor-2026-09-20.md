# Instruction refactor — 2026-09-20

Baseline revision: `2101f496ef60a3bef22438f212de666f8e5c5bde`.
Baseline snapshot: `/private/tmp/doomcube-instructions-20260920/baseline/`;
SHA-256 inventory and original status: sibling `manifest.json`.
Final snapshot: sibling `completed/` and `completed-manifest.json`.
Snapshots include the pre-existing untracked AGENTS.md. No unrelated work was staged.
These are local filesystem checkpoints, not pushed commits; retain them if temporary storage is cleaned.

## Findings and destinations

- AGENTS.md and CLAUDE.md were identical 16,440-byte copies. AGENTS.md is
  canonical; CLAUDE.md explicitly directs readers there without a policy copy.
- Original Critical Rules remain in AGENTS.md; Directory Structure and MCU
  Topology moved to configuration-map.md; Architecture & Conventions moved
  to macro-conventions.md; Project Overview, PRINT_START Flow and Known Quirks
  moved to operating-context.md. Section names remain for legacy lookup.
- Operating facts are carried-forward reports, not newly verified live health.
  Duplicate Spoolman status was consolidated. Historical audit examples now
  have an explicit context-only banner; setup guide already had one.
- SAVE_CONFIG removal advice conflicted with its no-edit rule. The prohibition
  remains; manual removal requires an explicit exception. No firmware policy
  or timeout setting changed. Removed the unsupported blanket claim that bed
  and motors carry no fire risk, retaining the deliberate timeout policy.
- Local docs index no longer creates an incidental cross-repository edit task.
- Inherited /Users/mjohnson/AGENTS.md contains @RTK.md; repository RTK.md is
  absent. Global guidance was not modified; resolution of that inherited
  reference is outside this repository refactor.
- Hidden .codex/config.toml was inventoried, left untouched and not copied.
  No assumptions were made about additional automatically loaded references.

## Verification and scenario desk review

- Changed Markdown links resolve; all original specialized sections have a
  destination; git diff --check passes. No printer configuration changed.
- Small macro edit: root -> macro conventions -> affected macro/callers;
  preserve units, timing, boolean and acceleration rules; validate relevant diff.
- Owned mmu/base/mmu_hardware.cfg edit: root distinguishes it from managed
  siblings; operating context supplies MMU constraints. Do not edit runtime vars.
- FPS or cutter change: root triggers operating evidence before editing;
  retain midpoint semantics and X-first clearance; physical results must be
  reported separately from static validation.
- Stale audit request: root and historical banner prevent treating examples
  as an active assignment or deployed calibration.
- Release with incomplete evidence: the requested scope governs deployment;
  report missing restart/physical evidence, never equate a static check with
  qualification. Documentation cleanup itself grants no deployment authority.

This was a local semantic/route desk review, not independent review or a fresh
agent-runtime test. No printer restart, flash, motion or physical print test ran.
Word/byte reduction measures the repository entry file, not total runtime context
or a proven model-quality improvement. Task references are explicitly read on
request; their automatic loading is neither required nor assumed.

Canonical entry: 16,440 -> 4,007 bytes (75.6% reduction), 507 words.
